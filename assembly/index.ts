export * from './convertFace'

// import { getContextById, CanvasRenderingContext2D } from 'as2d/assembly/index'

// var ctx: CanvasRenderingContext2D | null

// export function init(): void {
//   ctx = getContextById('main')
// }

// export function update(): void {
//   if (ctx === null) {
//     return
//   } // the context should be initialized
//   ctx.clearRect(0, 0, 800, 600)
//   ctx.fillText('Hello world!', 200, 200)
//   ctx.commit() // always remember to commit, or you will cause a memory leak
// }
